<div class="timeline-container">
  <% project.project_features.each do |feature| %>
    <div class="timeline-item">
      <strong><%= feature.name %></strong>
      <span>— <%= feature.start_date %> to <%= feature.end_date %></span>
    </div>
  <% end %>
</div>
